function mostrar()
{
	// declarar variables

	var contador=0;
	var numero 
	var minimo = 1;
	var maximo = 0;
	var respuesta='si';

	while(respuesta == "si"){
		numero = prompt("Ingrese un número.");
		numero = parseInt(numero);

		if(bandera){
			bandera = false;
			maximo = numero;
			minimo = numero;

	} 
		
	} if(numero > maximo){
		maximo = numero;

	} if(numero < minimo){
		minimo = numero;

	}


}//FIN DE LA FUNCIÓN